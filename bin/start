#!/usr/bin/env node

import { run } from "../dist/start.mjs";

try {
	await run();
} catch (error) {
	const message = error instanceof Error ? error.message : String(error);
	console.error(`[@stormory/react-router-server] ${message}`);
	process.exitCode = 1;
}
